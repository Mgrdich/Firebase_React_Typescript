export interface IStripe {
    background:string;
}

export interface LogoProps {
    linkTo: string;
    link?: boolean;
    height: number;
    width: number;
}



export interface LayoutProps {
    children:JSX.Element
}