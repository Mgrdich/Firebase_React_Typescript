import React from 'react';
import {ICompRoutes} from "../../Interfaces";
import {Redirect, Route} from "react-router";

const PubicRoutes: React.FC = () => {
    return(
        <>
            </>
    );
};

export default PubicRoutes;